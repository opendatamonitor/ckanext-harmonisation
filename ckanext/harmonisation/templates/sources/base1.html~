{% extends "page.html" %}

{% set pkg = c.pkg_dict %}

{% block breadcrumb_content_selected %} class="active"{% endblock %}

{% block breadcrumb_content %}
    {% set controller = 'ckanext.controllers.package:CustomHarmonisationController' %}
  {% if pkg %}
    {% set dataset = pkg.title or pkg.name %}
    {#{% if pkg.organization %}
      {% set organization = pkg.organization.title or pkg.organization.name %}
      <li>{% link_for _('Organizations'), controller='organization', action='index' %}</li>
      <li>{% link_for organization|truncate(30), controller='organization', action='read', id=pkg.organization.name %}</li>
    {% else %}
      <li>{% link_for _('Datasets'), controller='package', action='search' %}</li>
    {% endif %}
    <li{{ self.breadcrumb_content_selected() }}>{% link_for dataset|truncate(30), controller='package', 
        action='read', id=pkg.name %}</li>#}
  {% else %}
   {# <li> {{ h.nav_named_link(_('Harvest Sources'),
        'harvest'.format(c.dataset_type)) }}</li>   
    <li class="active">{{ h.nav_named_link(_('Create Custom MetaHarvest
        Source'), 'new_custom'.format(c.dataset_type)) }}</li> #}
  {#   <li>{% link_for _('Create Custom Metaharvest Source'), controller=controller, action='search' %}</li>
    <li class="active"><a href="">{{ _('Create Custom Metaharvest Source') }}</a></li>#}
    {% endif %}
{% endblock %}
